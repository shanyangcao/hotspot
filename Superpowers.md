**Superpowers（超能力）是专为AI 编程 Agent（Claude Code、Cursor、OpenCode 等）打造的开源工程化技能框架**，核心是一套**强制触发、可组合、标准化的 Skill（技能）库**，把资深工程师 30 年软件工程最佳实践，固化为 AI 必须遵守的自动化开发流程，解决传统 AI 写代码 “无规划、无测试、乱生成、质量差” 的痛点，让 AI 从 “初级实习生” 进化为 “守规矩的专业工程师”。

## 一、核心本质：什么是 Superpowers Skill？

Skill 是 Superpowers 的核心单元，**不是简单的提示词 / 插件 / 代码片段**，而是**封装了完整开发工作流的可复用方法论单元**，结构固定为：**Skill = 触发条件 + 标准化执行流程 + 质量门禁（强制校验） + 输出规范**

- 触发：AI**自动识别场景调用**，无需手动干预；
- 执行：严格按预设工程步骤推进，拒绝 AI “拍脑袋” 决策；
- 门禁：关键节点强制校验，不达标禁止进入下一步；
- 复用：一次定义，适配所有 AI 编程工具，跨项目通用。

## 二、核心设计铁律（Superpowers 的灵魂）

1. **流程优先，拒绝即兴**：强制按 “需求澄清→规划→开发→测试→审查→收尾” 闭环执行；
2. **TDD 绝对强制**：**无失败测试，绝不写生产代码**，直接删除违规生成的代码；
3. **证据驱动**：所有功能 / 修复必须验证通过，确认有效才算完成；
4. **复杂度最低**：优先简洁可维护的实现，拒绝冗余花哨逻辑。

## 三、14 个核心 Skill 全分类（四大类，覆盖开发全流程）

### 1. 规划协作类（开发前置，定方向、拆任务）

- **brainstorming（头脑风暴）**：苏格拉底式提问，澄清需求边界、明确设计方案，用户确认后才进入开发；
- **writing-plans（编写计划）**：拆解任务为**2-5 分钟微任务**，明确文件路径、代码片段、验证步骤；
- **using-git-worktrees（工作区隔离）**：自动创建独立 Git 分支 / 工作树，不污染主分支代码；
- **subagent-driven-development（子代理开发）**：每个任务启动独立子 Agent，上下文隔离，并行执行；
- **dispatching-parallel-agents（并行代理分发）**：多任务并发执行，提升开发效率；
- **finishing-a-development-branch（分支收尾）**：全量测试验证、清理临时文件，提供合并 / PR / 丢弃选项。

### 2. 测试质量类（核心保障，杜绝 Bug）

- **test-driven-development（TDD 测试驱动）**：强制**红→绿→重构**循环：先写失败测试→写最小代码通过测试→重构优化，全程锁死；
- **verification-before-completion（完成前验证）**：功能完成后，对照需求逐行校验，确认无误才算交付。

### 3. 调试排错类（Bug 修复，系统化排查）

- **systematic-debugging（系统化调试）**：4 步根因排查：定位症状→提出假设→验证假设→修复问题，找不到根因禁止改代码。

### 4. 代码审查类（质量兜底，规范合规）

- **requesting-code-review（发起代码审查）**：两阶段审查：①需求合规性审查 ②代码质量审查，严重问题直接阻断；
- **receiving-code-review（接收审查反馈）**：按评审意见修改代码，闭环反馈。

### 5. 框架元技能（扩展维护）

- **using-superpowers（框架使用）**：AI 加载 Superpowers 体系、理解 Skill 调用规则；
- **writing-skills（自定义技能）**：按规范新增自定义 Skill，扩展框架能力。

## 四、核心触发方式

1. **自动触发（主流）**：AI 识别用户指令场景，自动匹配 Skill，无需手动输入；

- - 例：“设计用户登录功能”→触发 brainstorming；“排查接口报错”→触发 systematic-debugging；

1. **手动触发**：输入`/技能名`（如`/tdd`、`/debug`），强制 AI 调用指定 Skill 执行。

## 五、与传统 AI 编程的核心区别

| **维度** | **传统 AI 编程**       | **Superpowers Skill**            |
| -------- | ---------------------- | -------------------------------- |
| 工作模式 | 单次对话、即兴生成     | 多轮迭代、流程强制驱动           |
| 质量保障 | 依赖人工审查、易出 Bug | 内置 TDD + 双阶段审查、自动校验  |
| 开发思维 | 无规划、凭经验         | 资深工程师工程方法论固化         |
| 适用场景 | 简单脚本、小功能       | 项目级开发、复杂系统、工业级交付 |

## 六、适配范围与价值

1. **适配工具**：Claude Code、Cursor、OpenCode、Codex、Gemini CLI 等所有主流 AI 编程 Agent；
2. **核心价值**：减少 80% 代码返工、提升代码可维护性、新手也能遵循工业级开发规范、统一团队开发标准。
![img](https://cdn.nlark.com/yuque/0/2026/jpeg/56763514/1775915580113-b3dd55bf-6ff7-4977-854b-011ae84133e9.jpeg)

所谓的 Harness（原意为“甲胄”或“马具”）并不是什么炫技的新名词，而是一套**将 LLM 的不确定性转化为系统稳定性**的工业级脚手架。它的核心价值在于：**拒绝让 Agent 在 Prompt 的“迷魂阵”里打转，而是通过外部环境（逻辑、存储、评估）的确定性，来约束和修正模型的随机性**。

主要三个核心理解点：

- **从“炼丹”到“工业化”：** Harness 的本质是**模块化解耦**。它将 Agent 的表现拆解为六个维度（上下文、工具、编排、记忆、评估、恢复）。当 Agent 表现不佳时，你不再是盲目地改 Prompt，而是像调试传统软件一样，去定位是哪一个模块的“零件”坏了。
- **状态管理的外部化：** 这是工程实现上的关键——**别相信模型的“脑容量”**。Harness 强调将任务进度、中间结论等状态从昂贵且不稳定的 Context Window 中剥离，外化到磁盘或 Redis。这种“外挂大脑”的设计才能支撑起长流程、跨天级别的复杂任务。
- **失败沉淀的进化逻辑：** 这是一套**诊断自愈体系**。Agent 犯错并不可怕，可怕的是你没有把修复逻辑“下沉”到环境里。通过 Trace、日志和 Eval 评估集，将每一次失败转化为系统边界的加固，这才是 Agent 能够真正上线的底层逻辑。

![img](https://cdn.nlark.com/yuque/0/2026/png/56763514/1775837786560-863ff840-6b83-4b23-a6be-7489e4317788.png)

图片里这张大图，讲的就是 **Harness Engineering**（简称 Harness 工程）。简单说，它就是给那些“记忆只有几小时”的 LLM 套上一套工程化的“马甲”和制度，让它们能靠谱地完成复杂、长期的任务，而不是干两下就忘、乱来或者半途而废。

#### 1. 先说清楚：为什么需要 Harness？

LLM 再聪明，也有个致命问题：

- 上下文窗口就那么长，每次新 Session 基本等于失忆。
- 容易自我审查，不敢干、不想干复杂的事。
- 长期任务？几小时一过，它就当没发生过。

Harness 的核心想法就是：**别指望模型自己变聪明，而是给它建一套完整的工作制度和支撑系统**。相当于把一个只有短期记忆的实习生，变成一个有规划、有记忆备份、有质检、有纠错的工程师团队。

目标很直白：让“几小时记忆的工程师”也能稳定交付复杂项目，而且尽量安全、不出乱子。

#### 2. Harness 要解决的七个关键点（图片里的关键结果）

图片把学习目标拆成了七块，核心就是这几个问题：

1. Harness 到底是个啥？
2. 它要解决 LLM 的哪些痛点？
3. 最终想达到什么效果？
4. 需要实现哪几个核心功能？
5. Claude Code 里是怎么实现的？
6. OpenClaw（或者类似开源实现）又是怎么玩的？
7. 自己要给一个新目标搭 Harness，大概走什么流程？

#### 3. Harness 必须有的六个核心功能

这是整张图最核心的部分。Harness 不是随便套个 prompt，而是要真正落地这六件事：

- **Planner（规划师）**：把大任务拆成小步骤，告诉“干活的”接下来该干啥。
- **Memory（记忆管理）**：提供合适的上下文、当前目标和进度，别让它瞎猜。
- **Evaluator（评估师）**：检查干活的结果好不好。
- **Corrector（纠错师）**：干得不好就直接指出来、改掉。
- **Constraint（约束）**：设定规则，不让它干危险或违规的事。
- **Evolution（进化/固化）**：把成功的经验记下来，下次直接复用。

这六个功能形成一个闭环：规划 → 执行 → 检查 → 纠错 → 固化经验。缺了哪一块，系统都容易崩。

#### 4. OpenClaw 风格的实现流程

OpenClaw 的做法比较直接，流程是这样的：

1. Agent 启动一个全新的 Session（因为上下文有限，必须定期重启）。
2. Planner 给它分配任务 + 说明下一步。
3. Memory 注入合适的上下文和历史。
4. Agent 老老实实干活。
5. Evaluator 跑一遍检查：

- - 干得好 → Evolution 把经验固化。
  - 干得烂 → Corrector 纠正，反馈再塞回上下文。

1. 如果上下文快满了，就结束当前 Session，带上关键记忆开新 Session。

整个过程像一个带自动纠错和记忆传递的循环，适合需要长期跑的任务。

#### 5. Claude Code 风格的实现（三种 Agent 协作）

Claude Code 的做法更像搭了一个小团队，核心是三种 Agent 配合：

- **规划 Agent**：负责拆任务、调度下一步。
- **干活 Agent**（Coding/Worker Agent）：真正写代码、干具体活。
- **质检 Agent**（Evaluator/Quality Agent）：检查输出、跑测试、找 Bug。

**实际跑起来的流程大概是这样**：

- 先基于 JSON 文件定义整体目标，建个 progress.txt 和 Git 仓库用来记录真实状态（这是持久化记忆的关键）。
- 干活 Agent 读进度和 Git 日志，了解当前做到哪了。
- 规划 Agent 挑下一个任务丢给干活 Agent。
- 干活 Agent 干完后，提交 Git，清空上下文，更新进度文件。
- 质检 Agent 接手检查，如果有问题就反馈回去。
- 上下文快爆了？总结关键信息，重开 Session，继续干。

三种 Agent 互相配合，加上 Git + 进度文件当“外部大脑”，就能撑起长期项目。图片里还画了初始化、循环、Bug 处理、Session 切换等细节。

#### 6. 自己搭 Harness 的大致流程

想给自己的项目搞一套 Harness，可以参考这些步骤：

1. 明确长期目标，把它拆成可追踪的任务清单。
2. 设计好上面那六个核心功能（规划、记忆、评估、纠错、约束、进化）。
3. 决定用哪种风格：是 OpenClaw 那种 Session + 六功能模式，还是 Claude Code 那种三 Agent + Git 持久化模式。
4. 实现记忆机制（用文件、Git、数据库等外部存储，弥补上下文窗口）。
5. 建反馈闭环（Evaluator + Corrector 必须强）。
6. 加上进化环节，把好经验固化成可复用模板。
7. 处理 Session 管理：上下文超长就总结关键点，重启新 Session。

#### 7. 最后小结

Harness Engineering 本质就是给 LLM 搭一个“操作系统”或者“工程管理制度”。模型负责思考，Harness 负责管住它、记住它、检查它、纠错它。

不管是 OpenClaw 还是 Claude Code，核心思想都一样：**别靠模型单打独斗，要用制度、工具和协作把它变成一个靠谱的长期生产力工具**。

现在 2026 年，Harness 已经是 AI Agent 领域的一个热词，很多大厂和社区都在往这个方向使劲。掌握了它，你就知道怎么把“聪明但不靠谱”的 LLM，变成能真正交付项目的“工程团队”。
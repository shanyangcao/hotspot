**LLM Wiki 是由大语言模型（LLM）自动维护、持续演化的结构化知识库（维基）范式**，核心是把零散原始资料 “编译” 成可累积、可链接、可追溯的 Markdown 维基，而非每次提问临时检索。2026 年 4 月由前特斯拉 AI 负责人 Andrej Karpathy 提出，被视为替代传统 RAG 的下一代知识管理方案。

### 一、核心架构（三层）

1. **raw/（原始资料层）**存放不可修改的源文件（文章、论文、PDF、笔记、图片等），作为唯一可信溯源依据，LLM 只读不写。
2. **wiki/（LLM 维护层，核心）**LLM 自动生成与更新的 Markdown 文件库，包含：

- 概念 / 实体 / 人物 / 事件独立词条
- 摘要、对比分析、时间线、分类索引
- 双向链接（[[wiki-link]]）与回链
- 矛盾标记、过时内容提醒、版本痕迹

1. **schema/（规则模板层）**定义维基结构、命名规范、链接规则、提示词模板，确保 LLM 生成风格统一、逻辑一致。

### 二、工作流（四步闭环）

1. **Ingest 导入**：把任意文件丢进 raw/，无需手动整理。
2. **Compile 编译**：LLM 一次性处理新内容，提炼概念、创建词条、建立链接、更新索引（只做一次，后续复用）。
3. **Query 查询**：提问时直接从 wiki/ 读取结构化内容，快速精准回答，无需重复扫描原始文档。
4. **Lint/Heal 自检**：定期扫描 wiki，自动合并重复概念、修复断链、标记冲突、归档过时信息，实现知识 “复利式增长”。

### 三、与传统 RAG 的关键区别

| **维度** | **传统 RAG**           | **LLM Wiki**                     |
| -------- | ---------------------- | -------------------------------- |
| 处理时机 | 查询时（每次从零检索） | 摄入时（一次编译，永久复用）     |
| 知识形态 | 临时片段拼接           | 持久化结构化维基                 |
| 链接关系 | 临时语义匹配           | 预构建双向链接网络               |
| 矛盾处理 | 易忽略或幻觉           | 主动标记并溯源                   |
| 适用场景 | 实时性强、高频更新     | 深度沉淀、长期学习、团队知识管理 |

简单类比：**RAG 像 “考试时临时翻书”，LLM Wiki 像 “提前把书整理成百科全书”**。

### 四、核心优势

- **知识沉淀**：从 “临时回答” 到 “永久知识库”，每次学习都在强化体系。
- **效率极高**：查询直接读结构化内容，速度提升数倍，无重复计算。
- **极简架构**：无需向量库、embedding 流水线，仅用 Markdown + LLM + 简单 UI/CLI。
- **可追溯可信**：所有结论均可反向链接到 raw/ 原始资料，便于核对与纠错。
- **自治演化**：LLM 持续维护，人工仅需补充原始资料与偶尔校验。

### 五、典型应用场景

- 个人学习：论文、笔记、书籍的结构化整理与关联
- 团队知识管理：项目文档、会议记录、技术手册的自治维护
- 研究辅助：文献综述、概念对比、研究脉络梳理
- 内容创作：素材库、灵感链接、结构化大纲生成
- 垂直知识库：佛学、古籍、医疗、法律等领域的专业维基

### 六、主流实现工具

- **llmwiki.app**：官方演示站，支持自定义知识库。
- **llmwiki（PyPI）**：开源 Python 包，本地部署，支持多模型。
- **Obsidian 插件**：结合双向链接笔记与 LLM 编译能力。
- **GitHub Gist 模板**：Karpathy 原始方案，可直接 Fork 定制。